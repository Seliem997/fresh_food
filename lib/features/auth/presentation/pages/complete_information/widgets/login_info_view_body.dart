import 'package:flutter/material.dart';

class LoginInfoViewBody extends StatelessWidget {
  const LoginInfoViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
